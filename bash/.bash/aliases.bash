# cd
alias ..='cd ..'

# commands starting with % for pasting from web
alias %=' '

# changing directory to code project
function c { cd ~/dev/$1; }

alias bash_cache_reset="hash -r"
alias bash_cache_reset_function="hash -d"
